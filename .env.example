# Example environment file. Copy to `.env` and fill in values.
# The Secrets Gradle Plugin reads `.env` at build time and exposes values
# as `BuildConfig.*` fields (see app/build.gradle.kts).
#
# This project currently does not require any secrets at build time;
# add entries here only when a feature needs them.
